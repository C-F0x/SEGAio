[package]
name = "revealer"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_System_Memory",
]}
